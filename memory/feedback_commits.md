---
name: Manual commits preference
description: User does all git commits manually — never commit on their behalf
type: feedback
---

User prefers to do all git commits manually.

**Why:** Saves tokens, keeps user in control of commit history.

**How to apply:** When work is ready to commit, stop at "ready to commit" and let the user handle it. Never run git add / git commit / git push unless explicitly asked.
